# Сводка родительского чата — бот

Telegram-бот, который читает родительские чаты в MAX и раз в сутки присылает выжимку:
что нужно сделать, сколько сдать денег, какие даты и до чего договорились.
Чатов может быть несколько — по каждому приходит своя сводка.

В отличие от [личной версии](../Max_connect), здесь всё делается внутри Telegram:
человек присылает код приглашения, номер телефона и код из SMS — и дальше сервис
работает сам. Ни GitHub, ни терминал пользователю не нужны.

Проект открытый, лицензия MIT — поднимайте свой экземпляр, дорабатывайте, делитесь.

## Состояние

Работает: онбординг целиком, несколько чатов на пользователя, ежедневные сводки
по каждому чату отдельно, утреннее напоминание о планах на день, внеочередная
сводка при всплеске активности, сводка и вопросы по запросу, приглашения ссылкой,
кнопочное меню, внятные сообщения о сбоях. Проверено на живом чате.

Картинки читаются выборочно: те, у которых есть подпись или про которые следом
спросили. Дальнейшее — в [ROADMAP.md](ROADMAP.md).

## Как устроено

```
Один процесс на сервере:
  ├─ aiogram — бот: онбординг и команды, один на всех пользователей
  ├─ PyMax   — вход в MAX и чтение истории, сессия на каждого
  ├─ зрение  — выборочное чтение картинок: подписанные и те, про которые спросили
  ├─ планировщик — вечерняя сводка, утреннее напоминание, слежение за всплесками
  └─ SQLite  — пользователи, их чаты, календарь событий, замеры активности
       └─► Kimi (или GigaChat) ─► сводка ─► в личку пользователю
```

Почему постоянный процесс, а не serverless: вход в MAX — это диалог на несколько
минут (номер → код из SMS → пароль), и всё это время соединение с MAX должно быть живо.

## Онбординг глазами пользователя

1. `/start` → бот просит код приглашения
2. Объясняет, какой доступ получает, и просит включить в MAX пароль для входа —
   без него MAX не пускает на новое устройство и шлёт коды в мобильное приложение
3. Номер телефона → код из SMS → пароль от MAX
4. Выбор чатов кнопками из списка групп — можно отметить несколько
5. Выбор времени сводки
6. Дальше молчит: вечером присылает выжимку по каждому чату, утром — короткое
   напоминание о том, что запланировано на день

## Команды

| Команда | Кому | Что делает |
|---|---|---|
| `/start` | всем | подключение или справка |
| `/summary`, `/summary 3` | пользователю | сводка за сутки или за N дней |
| `/q вопрос` | пользователю | ответ по переписке за месяц |
| `/plan` | пользователю | что записано на ближайшие две недели |
| `/settings` | пользователю | список чатов, время сводки, утреннее напоминание, пауза |
| `/stop` | пользователю | удалить сессию и все данные |
| `/invite` | админу | ссылка-приглашение, одноразовая |
| `/users` | админу | кто подключён и на каком шаге застрял |
| `/health` | админу | у кого что сломалось |
| `/stats`, `/stats 7` | админу | расход на модель по людям и видам работы |
| `/provider all kimi` | админу | переключить модель всем или одному |

## Запуск

```bash
python3 -m venv .venv && ./.venv/bin/pip install -r requirements.txt
cp .env.example .env    # заполнить
set -a && source .env && set +a
./.venv/bin/python -m app.main
```

На сервере — через systemd, чтобы процесс поднимался после перезагрузки.

## Переменные окружения

| Переменная | Зачем |
|---|---|
| `BOT_TOKEN` | токен бота от @BotFather |
| `ADMIN_IDS` | telegram id админов через запятую |
| `SECRET_KEY` | ключ шифрования сессий MAX. **Потеряете — все подключения придётся делать заново** |
| `LLM_PROVIDER` | `kimi` (по умолчанию) или `gigachat` |
| `KIMI_API_KEY`, `KIMI_MODEL` | доступ к Kimi |
| `GIGACHAT_AUTH_KEY` | запасной провайдер |
| `TIMEZONE`, `DEFAULT_DIGEST_TIME` | по умолчанию Москва и 20:00 |
| `DEFAULT_MORNING_TIME` | утреннее напоминание, по умолчанию 07:30 |
| `DATA_DIR` | где лежат база и сессии, по умолчанию `data/` |
| `BURST_ENABLED` | слежение за всплесками, по умолчанию включено |
| `BURST_MIN_MESSAGES`, `BURST_FACTOR` | пороги всплеска: 25 сообщений за час и вчетверо больше обычного |
| `BURST_COOLDOWN_HOURS`, `QUIET_HOURS` | пауза после срабатывания (6 ч) и тихие часы (`23-8`) |
| `VISION_ENABLED`, `VISION_MODEL` | чтение картинок, по умолчанию включено на `kimi-k2.6` |
| `VISION_MAX_PHOTOS` | сколько картинок смотрим на одну сводку, по умолчанию 6 |
| `PRICE_IN`, `PRICE_OUT` | цена за миллион токенов; без них `/stats` покажет только токены |

## Календарь событий

Сводка живёт сутками: что не обсуждали вчера, для неё не существует. В живом чате
про поднятие флага 7 сентября сказали один раз — 28 августа, за десять дней. Такое
теряется, и никакая суточная выжимка это не лечит.

Поэтому события с разобранной датой складываются в таблицу и живут два месяца.
Накануне бот сам достаёт их в блок «завтра», а утреннее напоминание собирается
из календаря, а не из вчерашней сводки. Новых уведомлений у пользователя
не появляется — просто в привычных сообщениях перестаёт теряться давнее.

Одно событие в переписке уточняют по нескольку дней подряд и разными словами,
поэтому при чтении дубли склеиваются: короткий запрос к модели оставляет самую
свежую формулировку. Если модель недоступна, гасятся хотя бы дословные повторы.

Посмотреть, что бот запомнил, — команда `/plan` или кнопка «🔭 Что впереди».
Календарь наполняется из сводок, поэтому свежему подключению полезно один раз
попросить `/summary 7`: недельная сводка разложит по датам всё, о чём в чате
успели сказать заранее.

Напоминаний «за час до» сознательно нет — почему, написано в [ROADMAP.md](ROADMAP.md).

## Всплески активности

Раз в час бот считает, сколько сообщений пришло в чат, и сравнивает с обычным для
этого чата. Если сообщений разом стало вчетверо больше обычного и при этом не меньше
двадцати пяти — присылает сводку, не дожидаясь вечера. Модель для самого замера
не нужна, это просто счёт сообщений.

Пороги нарочно высокие, а после срабатывания бот молчит шесть часов и не тревожит
ночью: сервис, который шлёт лишнее, выключают быстрее, чем тот, который иногда
опаздывает. Первые сутки работы порог только абсолютный — сравнивать ещё не с чем.
Выключается кнопкой в `/settings`.

## Чтение картинок

В родительском чате расписание и домашнее задание нередко присылают скриншотом.
Но замер за неделю показал, что четыре пятых фотографий — букеты и снимки
с праздника, читать там нечего. Поэтому смотрим не на всё подряд: в модель
со зрением уходят картинки с подписью и те, про которые следом задали вопрос
(«по английскому такую тетрадь надо?»). Не больше шести на одну сводку,
описания складываются в базу — одна и та же картинка не оплачивается дважды.

Отдельно про личные данные. Первая же проверка на живом чате вытащила таблицу
с именами детей, днями рождения и телефонами родителей, и модель без ограничений
переписала её целиком. В промпте `app/vision.py` стоит прямой запрет: такие
списки описываются одной строкой «список класса с контактами», без содержимого.
Если поднимаете свой экземпляр — не ослабляйте это правило.

## Присмотр за сервисом

Пять админских команд, доступны тем, чьи id перечислены в `ADMIN_IDS`.

**`/invite`** выдаёт одноразовую ссылку вида `t.me/бот?start=код`. Если ссылки
у человека не открываются, тот же код можно ввести в бот вручную.

**`/users`** показывает не состояние из базы, а место в воронке словами:
«не начал подключение MAX», «ввёл номер, ждёт код», «вошёл, не выбрал чат»,
«работает». Это важнее, чем кажется: подключение упирается в чужое приложение,
и люди застревают. На первых приглашённых все трое остановились на самом первом
шаге — заводить пароль для входа в MAX, — и по слову «new» это читалось хуже.

**`/health`** перечисляет тех, у кого сбои идут подряд, с текстом последней
ошибки. Ждать этой команды не обязательно: при фатальном сбое у любого
пользователя, а также на третьем сбое подряд, админу приходит сообщение само.

**`/stats`** отвечает на вопрос «кто сжёг токены». Обращений к модели на одну
сводку несколько: сама сводка, склейка по каждой задетой дате, описание каждой
картинки. Все они складываются в счёт операции и попадают в таблицу `usage`.
Отчёт идёт по людям и по видам работы — сводки, вопросы, утренние напоминания.
`/stats 7` — за неделю, по умолчанию за месяц.

Деньги показываются, только если заданы `PRICE_IN` и `PRICE_OUT` — цена
за миллион токенов у выбранной модели. Без них в отчёте будут одни токены:
выдуманная цифра в деньгах хуже, чем её отсутствие. Прайс в код не зашит
намеренно — он меняется, а тихо устаревшая цена вводит в заблуждение.

**`/provider`** переключает модель одному человеку или всем сразу:
`/provider all kimi`, `/provider 12345 gigachat`, `/provider all default`.
Пригодится, если основной провайдер лёг.

## Про данные и доверие

Сервис хранит **сессии чужих аккаунтов MAX**. Такая сессия — это доступ к мессенджеру
целиком, а не к одному чату. Это следствие архитектуры: бот читает чат от имени
пользователя, значит нужен доступ пользователя. Никакими настройками это не сужается —
сузить может только другая архитектура, где чат читает бот, добавленный в него админом.

Отсюда решения в коде:

- сессии лежат на диске зашифрованными (`app/crypto.py`), расшифровываются во временный
  каталог только на время работы с MAX и стираются после;
- база и сессии создаются с правами `600`, каталоги — `700`; шифрование защищает
  от утёкшего диска, права — от соседа по машине, нужно и то и другое;
- `/stop` удаляет всё, что привязано к человеку: сессию, настройки, чаты, календарь,
  учёт расхода и описания прочитанных картинок;
- на первом экране бот прямо говорит, какой доступ получает и куда уходят тексты;
- вход только по приглашению — посторонние не могут отдать свои аккаунты.

**Чего эти меры не делают.** Ключ шифрования лежит в `.env` на той же машине, поэтому
от владельца сервера они не защищают — и не должны притворяться, что защищают. У того,
кто держит экземпляр, есть полный доступ к аккаунтам MAX всех его пользователей.
Говорите об этом людям прямо: обещание, которое нельзя проверить, хуже честного
предупреждения.

### Что где лежит

| Где | Что | Как |
|---|---|---|
| `data/sessions/*.enc` | сессии MAX | зашифровано (Fernet), права 600 |
| `data/bot.db` | телефон, названия чатов, календарь событий, описания картинок, тексты вопросов | открытым текстом, права 600 |
| `backups/*.tgz` | копия обоих за 7 дней | права 600, на той же машине |
| нигде | сырые сообщения чата | читаются, уходят в модель, забываются |

### Развёртывание

В `deploy/` лежат юниты systemd: сама служба и ежедневная резервная копия.
У службы отключены лишние права (`ProtectSystem=strict`, `PrivateTmp`,
`NoNewPrivileges`), запись разрешена только в `data` и `backups`.

Тексты сообщений уходят в языковую модель. У Kimi это Moonshot (Китай), у GigaChat —
Сбер (Россия). В родительских чатах бывают имена детей и телефоны родителей —
пользователи должны это понимать, поэтому в промпте стоит запрет тащить
персональные контакты в сводку.

## Ответственность

Сервис работает через **неофициальный внутренний API MAX** (библиотека PyMax). Это
не запрещено технически, но формально нарушает условия использования мессенджера,
и MAX вправе ограничить или заблокировать аккаунт. Документированные причины
блокировок связаны с рассылками и спамом — режим «только чтение», в котором работает
бот, у самого MAX отнесён к минимальному риску, но гарантий никто не даёт.

Поднимая свой экземпляр, вы становитесь оператором чужих данных: у вас на диске
оказываются сессии MAX ваших пользователей, то есть доступ к их мессенджерам целиком.
Скажите им об этом прямо — в текстах бота это уже сделано, не убирайте.

Автор не несёт ответственности за последствия использования. Лицензия MIT.

## Ограничения

- PyMax работает через неофициальный внутренний API MAX: он может измениться,
  и формально это нарушает условия сервиса.
- Из картинок читаются только подписанные и те, про которые следом спросили.
  Остальные бот просто помечает как фото — по замеру четыре пятых снимков
  в родительском чате это букеты и праздник.
- Несколько аккаунтов с одного адреса — заметный для MAX паттерн. Сводки разнесены
  по времени, между запросами паузы, но риск блокировки аккаунта ненулевой.
